@model IEnumerable<TrashCollector.Models.PickUpDay>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Day.DayChoice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.day1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.day2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Weeks)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Day.DayChoice)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.day1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.day2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Weeks)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
    break;
}

</table>
